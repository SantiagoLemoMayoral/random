apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: helpdesk-cluster-reader-binding

subjects:
  - kind: ServiceAccount
    name: helpdesk-app
    namespace: prod

roleRef:
  kind: ClusterRole
  name: helpdesk-cluster-reader
  apiGroup: rbac.authorization.k8s.io